dist
node_modules
.serena
# vendored git submodule (third-party code, not ours to format)
vendor
package-lock.json

# Hand-crafted markdown — keep current formatting
*.md

# Fleet-canonical Dependabot config — keep byte-identical to the template
.github/dependabot.yml
