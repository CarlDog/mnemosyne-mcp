# Where the Vite dev server proxies /api requests to. Default:
# http://127.0.0.1:3010 (matches the root project's dev MCP_PORT default
# suggestion). Only read by vite.config.ts, and only during `npm run dev`.
# MNEMO_WEBUI_API_PROXY=http://127.0.0.1:3010
